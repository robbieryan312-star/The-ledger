/**
 * verify-lobbying-votes.ts — cross-check lobbying vote links against
 * any matching official Congress vote in the generated snapshot.
 *
 * Run with: npm run verify:lobbying-votes
 *
 * NOTE: The former mock lobbying data (DNU quarantine) has been deleted.
 * This script will be re-wired when a real lobbying pipeline is built.
 */

console.log(
  'PASS: no lobbying data pipeline yet — no vote cross-checks to run. Will re-activate when real lobbying data is integrated.',
);
