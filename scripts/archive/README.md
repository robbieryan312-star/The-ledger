# Archived scripts

Superseded one-off migration tools. Use `npm run profile:build` instead.

| File | Superseded by |
|------|----------------|
| `migrate-s000033-profile.ts` | `profile:build -- --members S000033` |
| `migrate-gold-profiles-batch1.ts` | `profile:build` batch protocol |
