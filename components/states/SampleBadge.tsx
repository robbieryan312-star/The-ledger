/** Marks dashboard figures sourced from a small verified sample batch (not full corpus). */
export default function SampleBadge() {
  return (
    <span className="ml-1.5 font-mono text-[9px] uppercase tracking-wide text-[var(--muted)] border border-[var(--border-default)] rounded px-1 py-px">
      sample
    </span>
  );
}
