# npm audit log — CONSOLIDATED BRIEF v2 Phase B

**Date:** 2026-07-18  
**Branch:** `cursor/fl-state-locked-spec-70a6`  
**Command:** `npm audit` / `npm audit fix` (no `--force`)

## Outcome

`npm audit fix` (safe, non-breaking) applied **no** dependency upgrades — remaining
issues require `--force` and introduce breaking major downgrades.

## Remaining (7 vulnerabilities — not applied)

| Package chain | Severity | Why not fixed |
|---------------|----------|---------------|
| `react-simple-maps` → `d3-color` / `d3-interpolate` / `d3-zoom` | high | `npm audit fix --force` would install `react-simple-maps@1.0.0` (breaking) |
| `next` → `postcss` | moderate | `npm audit fix --force` would install `next@9.3.3` (breaking downgrade) |

## Follow-up

Track upstream Next.js / react-simple-maps releases; do not force-downgrade the app stack.
